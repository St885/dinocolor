# libs/ — (reservado)

Carpeta para librerías de terceros **vendorizadas** (incluidas manualmente en el repo) si
en el futuro se decide no depender de `npm` para alguna de ellas.

Actualmente **vacía**: DinoColor gestiona sus dependencias con `npm` (`package.json`) y
Vite. No añadir librerías aquí sin acordar la estrategia (ver `CLAUDE.md`).
